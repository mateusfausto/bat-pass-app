import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  title: {
    resizeMode: 'contain',
    textAlign: 'center',
    fontWeight: 'bold',
    fontSize: 28,
    color:'#ffbf00'
  },

  image: {
    resizeMode: 'contain',
    height: 180,
  }
});